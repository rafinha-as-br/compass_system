# Execution State — CPS-162

Estado: EM_EXECUÇÃO
Branch: feat/CPS-162-claude (criada a partir de develop 84602c3)
Atualizado em: 2026-09-18

## Objetivo
Implementar no compass-api o módulo `company` (multi-tenancy): entidade Company
(name, cnpj, domain único, plan FREE/BASIC/PRO), AgentUser ganha company + role
(OWNER/MEMBER) + createdAt; endpoints /api/companies/me, /me/agents,
/me/agents/invite, DELETE /me/agents/{id}, PUT /me/agents/{id}/role.

## Decisões tomadas com Rafinha (não voltar a perguntar)
- GET /me/agents aceita OWNER e MEMBER (payload igual: não há campo sensível no MVP).
- DTO do agente inclui `memberSince` (createdAt). `packagesCreated` ADIADO para CPS-130
  (TravelPackage não existe; Travel/Itinerary não têm FK de agente).
- Remoção de agente = hard delete do AgentUser + filtro JWT passa a rejeitar token
  de agente inexistente (invalida acesso). Sem FK em Travel/Itinerary, nada cascateia.
- Empresa é criada via POST /api/auth/cadastrar/agente com objeto `company` opcional
  (agente vira OWNER). Sem isso não haveria como existir empresa (spec não previa).
- Login gerado: localPart normalizado (NFD, minúsculo, espaços/-/_ → ".", só [a-z0-9.]),
  sufixo numérico a partir de 2 em colisão. Coluna do domínio = `email_domain`
  (DOMAIN é keyword no H2).
- Integridade no login de agente: sufixo do e-mail deve bater com company.domain.

## Concluído
- Implementação: company/{Company, PlanType, AgentRole, CompanyRepository,
  CompanyService, CompanyController}, exceptions/ForbiddenException (+handler 403),
  AgentUser (company EAGER, role, createdAt), AgentUserRepository, AuthController
  (cadastro com empresa, integridade no login), JwtAuthenticationFilter.
- Testes: CompanyServiceTest (7), CompanyControllerTest (22); Travel/Dashboard tests
  ajustados para criar o agente do token. Suíte completa: 61/61 OK.

## Próxima ação
code review automatizado (5.3c) → gate final (5.4) → commit/push/PR (5.5–5.6)

## Bloqueios / decisões pendentes de Rafinha
- nenhum
