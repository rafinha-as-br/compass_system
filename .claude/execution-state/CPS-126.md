Estado: EM_EXECUÇÃO

Issue: CPS-126 — Campo de observações da viagem
Branch: feat/CPS-126-claude (nova, a partir de develop)
GitHub Issue: https://github.com/rafinha-as-br/compass_system/issues/114

Objetivo: campo de observações ponta a ponta (texto livre, escopo da
viagem inteira). Backend (Travel.observations), preenchimento na criação
da rota (RouteCraft), exibição para o agente (Travel Matrix). Não editável
após a criação.

Achado importante durante os testes: o endpoint genérico PUT /travels/{id}
(full replace, usado por Travel Matrix em markAsReady) apagava
observations quando o payload não o carregava — corrigido preservando o
valor existente no servidor (incoming.setObservations(original)),
tornando a imutabilidade uma garantia do backend, não só uma omissão de UI.

Arquivos:
- compass-api: Travel.java (campo+getter/setter), TravelController
  (upsertParticipants preserva observations no full update) + teste
- routecraft_app: Travel entity, TravelDTO, RouteCreationController
  (observationsController), route_creation_page.dart (campo no passo de
  interesses + bloco na revisão), AppTextField (novo param maxLines)
- travel_matrix: Travel entity, TravelDTO, TravelViewModel (round-trip),
  RouteViewTab (exibição para o agente)
- l10n: routeCreationObservations*/observationsLabel/noObservations

Próxima ação: confirmar (via CI Linux) que os goldens route_creation/
route_creation_review não mudaram; finalizar commit/push/PR.
