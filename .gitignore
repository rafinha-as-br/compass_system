
## for ai context
/contexts
.claude

## for flutter context
.idea/
.vscode

## for python scripts (compass-api/scripts)
__pycache__/
*.pyc


# Release requests: registro operacional local, nao historico do produto (isso e' o release-manifest.yml)
.release/requests/

# Staging local do orchestrate.ps1 (dist/staging + artifacts baixados): saida
# efemera, o artefato real e' o ZIP que vai pro Drive, nao o git.
.release/dist/
