server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Flutter web usa rotas sem hash: qualquer caminho desconhecido cai no index.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
