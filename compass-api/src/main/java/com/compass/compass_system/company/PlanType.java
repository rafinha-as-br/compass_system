package com.compass.compass_system.company;

/**
 * Plano contratado pela empresa. Apenas exibição no MVP — não existe billing
 * nem aplicação de limites de uso por plano.
 */
public enum PlanType {
    FREE,
    BASIC,
    PRO
}
